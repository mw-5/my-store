/**
 * @description Contains information about a product
 * and its ordered quantity.
 */
export type Product = {
	id: number;
	name: string;
	price: number;
	url: string;
	description: string;
	amount: number;
};
